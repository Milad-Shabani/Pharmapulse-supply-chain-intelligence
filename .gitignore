__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.venv/
venv/
.DS_Store
.idea/
.vscode/

# Note: data/raw/, data/processed/, and reports/ are intentionally
# NOT ignored - all generated artifacts here are only a few MB total,
# so they're committed to keep the repo fully browsable on GitHub
# without anyone needing to run the pipeline first.
*.draft.md
